export { Header } from './header/header';
export { ApiLoader, Loader } from './loaders';
export { Modal } from './shared/modal'; 

// fecha y hora || situacion concreta || idea irracional || emocion || pensamiento racional || emocion